import React from 'react'
import { View, Text } from 'react-native'

const Total = ({ total }) => {
    return (
        <View>
            <Text>Total: {total}</Text>
        </View>
    )
}

export default Total;